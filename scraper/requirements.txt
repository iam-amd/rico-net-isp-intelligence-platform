# ── Core Scraper ──────────────────────────────────────────
playwright>=1.42.0
playwright-stealth>=2.0.2
beautifulsoup4>=4.12.0
pandas>=2.2.1
SQLAlchemy>=2.0.28
python-dotenv>=1.0.1
psycopg2-binary>=2.9.0  # PostgreSQL driver for sync_daemon

# ── CAPTCHA solving (free, local, no API key) ────────────
ddddocr>=1.4.0           # Free local CAPTCHA solver (offline, no API key)
pillow>=10.0.0           # Image preprocessing for CAPTCHA
# anthropic>=0.25.0     # Optional: Claude Vision fallback (only if ANTHROPIC_API_KEY set)

# ── Dashboard & Scheduler ────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
psutil>=5.9.0
apscheduler>=3.10.0
